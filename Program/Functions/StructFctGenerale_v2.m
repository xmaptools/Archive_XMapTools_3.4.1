function [Si] = StructFctGenerale_v2(Data,handles)
% -
% This function is hard-coded in XMapTools as an internal subroutine
% It calculates the structural formula based on all all the element maps
% available. 
%
% N Oxygens (defined by user)
%
% Created by P. Lanari (March 2020) 
%

Si = [];

return










